import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.resolve('dist'),htmls=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);e.isDirectory()?walk(p):e.name.endsWith('.html')&&htmls.push(p)}}walk(root);let checked=0;for(const file of htmls){if(file.endsWith('privacy.html'))continue;const html=fs.readFileSync(file,'utf8');let route=file.endsWith('404.html')?'/404/':'/'+path.relative(root,path.dirname(file)).replaceAll(path.sep,'/').replace(/^\.$/,'').replace(/\/?$/,'/');if(route==='//')route='/';for(const [,url]of html.matchAll(/<(?:script|link)[^>]+(?:src|href)="([^"]+)"/g)){if(!url.includes('/assets/'))continue;const resolved=new URL(url,'https://doomsday.cfd'+route).pathname;if(!fs.existsSync(path.join(root,resolved)))throw Error(`${file}: missing ${resolved}`)}const canonical=html.match(/rel="canonical" href="([^"]+)"/)?.[1];if(canonical!==`https://doomsday.cfd${route}`)throw Error(`${file}: canonical ${canonical} != ${route}`);checked++}console.log(`✓ ${checked} generated HTML files resolve assets and canonical URLs`)
